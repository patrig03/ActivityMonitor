<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:ActivityMonitor.Views"
             xmlns:viewModels="clr-namespace:ActivityMonitor.ViewModels"
             xmlns:lvc="using:LiveChartsCore.SkiaSharpView.Avalonia"
             x:DataType="viewModels:DashboardViewModel"
             x:Class="ActivityMonitor.Views.DashboardView"
             Foreground="White">

  <UserControl.Resources>
    <DataTemplate x:Key="InsightTemplate" DataType="viewModels:DashboardInsight">
      <Border Width="260"
              Margin="0,0,14,14"
              CornerRadius="20"
              Padding="18"
              Background="#111B2D"
              BorderBrush="#24324A"
              BorderThickness="1">
        <StackPanel Spacing="8">
          <TextBlock Text="{Binding Title}" FontSize="15" FontWeight="SemiBold" Foreground="#F7FAFF"/>
          <TextBlock Text="{Binding Detail}" Foreground="#8EA0C3" TextWrapping="Wrap"/>
        </StackPanel>
      </Border>
    </DataTemplate>

    <DataTemplate x:Key="BreakdownTemplate" DataType="viewModels:DashboardBreakdownItem">
      <Border Margin="0,0,0,10"
              CornerRadius="16"
              Padding="14"
              Background="#111B2D"
              BorderBrush="#24324A"
              BorderThickness="1">
        <Grid ColumnDefinitions="*,Auto">
          <StackPanel Spacing="4">
            <TextBlock Text="{Binding Label}" FontSize="14" FontWeight="SemiBold" Foreground="#F7FAFF"/>
            <TextBlock Text="{Binding Secondary}" Foreground="#8EA0C3" TextWrapping="Wrap"/>
          </StackPanel>
          <TextBlock Grid.Column="1"
                     Text="{Binding Duration}"
                     Foreground="#7DD3FC"
                     FontSize="18"
                     FontWeight="Bold"
                     VerticalAlignment="Center"/>
        </Grid>
      </Border>
    </DataTemplate>

    <DataTemplate x:Key="ThresholdTemplate" DataType="viewModels:DashboardThresholdState">
      <Border Margin="0,0,0,10"
              CornerRadius="16"
              Padding="14"
              Background="#111B2D"
              BorderBrush="#24324A"
              BorderThickness="1">
        <StackPanel Spacing="8">
          <Grid ColumnDefinitions="*,Auto">
            <TextBlock Text="{Binding TargetName}" FontSize="14" FontWeight="SemiBold" Foreground="#F7FAFF"/>
            <Border Grid.Column="1"
                    Background="#1E2A40"
                    CornerRadius="999"
                    Padding="10,4">
              <TextBlock Text="{Binding StateLabel}" Foreground="#C7D6F6" FontSize="11" FontWeight="SemiBold"/>
            </Border>
          </Grid>
          <ProgressBar Minimum="0"
                       Maximum="100"
                       Value="{Binding ProgressValue}"
                       Height="8"/>
          <TextBlock Text="{Binding UsageSummary}" Foreground="#F7FAFF" FontWeight="SemiBold"/>
          <TextBlock Text="{Binding LimitSummary}" Foreground="#8EA0C3"/>
        </StackPanel>
      </Border>
    </DataTemplate>

    <DataTemplate x:Key="DomainTemplate" DataType="viewModels:DashboardDomainItem">
      <Border Margin="0,0,0,10"
              CornerRadius="16"
              Padding="14"
              Background="#111B2D"
              BorderBrush="#24324A"
              BorderThickness="1">
        <Grid ColumnDefinitions="*,Auto">
          <StackPanel Spacing="4">
            <TextBlock Text="{Binding Domain}" FontSize="14" FontWeight="SemiBold" Foreground="#F7FAFF"/>
            <TextBlock Text="{Binding Share}" Foreground="#8EA0C3"/>
          </StackPanel>
          <TextBlock Grid.Column="1"
                     Text="{Binding Visits}"
                     Foreground="#8BFFB1"
                     FontWeight="Bold"
                     VerticalAlignment="Center"/>
        </Grid>
      </Border>
    </DataTemplate>

    <DataTemplate x:Key="InterventionTemplate" DataType="viewModels:DashboardInterventionItem">
      <Border Margin="0,0,0,10"
              CornerRadius="16"
              Padding="14"
              Background="#111B2D"
              BorderBrush="#24324A"
              BorderThickness="1">
        <StackPanel Spacing="6">
          <Grid ColumnDefinitions="*,Auto">
            <TextBlock Text="{Binding TargetName}" FontSize="14" FontWeight="SemiBold" Foreground="#F7FAFF"/>
            <TextBlock Grid.Column="1" Text="{Binding TriggeredAt}" Foreground="#8EA0C3"/>
          </Grid>
          <TextBlock Text="{Binding Status}" Foreground="#FBBF24" FontWeight="SemiBold"/>
          <TextBlock Text="{Binding Detail}" Foreground="#8EA0C3" TextWrapping="Wrap"/>
        </StackPanel>
      </Border>
    </DataTemplate>
  </UserControl.Resources>

  <ScrollViewer VerticalScrollBarVisibility="Auto">
    <StackPanel Margin="18" Spacing="18">

      <Border CornerRadius="28"
              Padding="24"
              Background="#10192A"
              BorderBrush="#24324A"
              BorderThickness="1">
        <Grid ColumnDefinitions="*,320" ColumnSpacing="18">

          <StackPanel Spacing="10">
            <TextBlock Text="Dashboard" FontSize="30" FontWeight="Bold" Foreground="#F7FAFF"/>
            <TextBlock Text="{Binding DashboardSubtitle}" Foreground="#8EA0C3" FontSize="14" TextWrapping="Wrap"/>

            <WrapPanel Orientation="Horizontal">
              <Border Margin="0,10,10,0"
                      Background="#17233A"
                      CornerRadius="999"
                      Padding="12,6">
                <TextBlock Text="{Binding MonitoringCadence}" Foreground="#D6E4FF" FontWeight="SemiBold"/>
              </Border>
              <Border Margin="0,10,10,0"
                      Background="#17233A"
                      CornerRadius="999"
                      Padding="12,6">
                <TextBlock Text="{Binding SnapshotSummary}" Foreground="#D6E4FF" FontWeight="SemiBold"/>
              </Border>
              <Border Margin="0,10,0,0"
                      Background="#17233A"
                      CornerRadius="999"
                      Padding="12,6">
                <TextBlock Text="{Binding LastRefreshLabel}" Foreground="#D6E4FF" FontWeight="SemiBold"/>
              </Border>
            </WrapPanel>
          </StackPanel>

          <Border Grid.Column="1"
                  CornerRadius="22"
                  Padding="18"
                  Background="#17233A"
                  BorderBrush="#30415D"
                  BorderThickness="1">
            <StackPanel Spacing="14">
              <StackPanel Orientation="Horizontal" Spacing="12">
                <Border Width="56"
                        Height="56"
                        CornerRadius="18"
                        Background="#223250">
                  <TextBlock Text="{Binding ProfileInitials}"
                             HorizontalAlignment="Center"
                             VerticalAlignment="Center"
                             FontWeight="Bold"
                             FontSize="20"
                             Foreground="#F7FAFF"/>
                </Border>
                <StackPanel Spacing="2" VerticalAlignment="Center">
                  <TextBlock Text="{Binding DisplayName}" FontSize="16" FontWeight="SemiBold" Foreground="#F7FAFF"/>
                  <TextBlock Text="Profil principal de monitorizare" Foreground="#8EA0C3"/>
                </StackPanel>
              </StackPanel>

              <Grid ColumnDefinitions="*,Auto">
                <StackPanel Spacing="4">
                  <TextBlock Text="Coada de atentie" Foreground="#8EA0C3"/>
                  <TextBlock Text="Presiune pe praguri si alerte recente" Foreground="#F7FAFF" FontWeight="SemiBold"/>
                </StackPanel>
                <Border Grid.Column="1"
                        Background="#223250"
                        CornerRadius="999"
                        Padding="12,6"
                        VerticalAlignment="Center">
                  <TextBlock Text="{Binding AttentionBadge}" Foreground="#F7FAFF" FontWeight="Bold"/>
                </Border>
              </Grid>

              <Button Command="{Binding RefreshCommand}"
                      Background="#7DD3FC"
                      Foreground="#08111F"
                      BorderThickness="0"
                      CornerRadius="14"
                      Padding="16,12"
                      HorizontalAlignment="Stretch">
                <TextBlock Text="Reincarca Dashboardul" FontWeight="Bold" HorizontalAlignment="Center"/>
              </Button>
            </StackPanel>
          </Border>

        </Grid>
      </Border>

      <views:QuickStatsBar/>

      <Border CornerRadius="24"
              Padding="20"
              Background="#10192A"
              BorderBrush="#24324A"
              BorderThickness="1">
        <StackPanel Spacing="14">
          <TextBlock Text="Ce iese in evidenta" FontSize="20" FontWeight="Bold" Foreground="#F7FAFF"/>
          <ItemsControl ItemsSource="{Binding Insights}" ItemTemplate="{StaticResource InsightTemplate}">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <WrapPanel Orientation="Horizontal"/>
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
          </ItemsControl>
        </StackPanel>
      </Border>

      <Grid ColumnDefinitions="*,*" ColumnSpacing="18">

        <Border CornerRadius="24"
                Padding="18"
                Background="#10192A"
                BorderBrush="#24324A"
                BorderThickness="1">
          <StackPanel Spacing="12">
            <TextBlock Text="Dinamica categoriilor" FontSize="18" FontWeight="Bold" Foreground="#F7FAFF"/>
            <TextBlock Text="Categorii principale dupa ore monitorizate." Foreground="#8EA0C3"/>
            <lvc:CartesianChart Series="{Binding CategoryUsageSeries}"
                                XAxes="{Binding CategoryXAxis}"
                                MinHeight="280"/>
          </StackPanel>
        </Border>

        <Border Grid.Column="1"
                CornerRadius="24"
                Padding="18"
                Background="#10192A"
                BorderBrush="#24324A"
                BorderThickness="1">
          <StackPanel Spacing="12">
            <TextBlock Text="Pondere procese" FontSize="18" FontWeight="Bold" Foreground="#F7FAFF"/>
            <TextBlock Text="Unde se concentreaza timpul monitorizat intre procese." Foreground="#8EA0C3"/>
            <lvc:PieChart Series="{Binding ProcessShareSeries}"
                          MinHeight="280"/>
          </StackPanel>
        </Border>

      </Grid>

      <Grid ColumnDefinitions="*,*" ColumnSpacing="18">

        <Border CornerRadius="24"
                Padding="18"
                Background="#10192A"
                BorderBrush="#24324A"
                BorderThickness="1">
          <StackPanel Spacing="12">
            <TextBlock Text="Activitate ferestre" FontSize="18" FontWeight="Bold" Foreground="#F7FAFF"/>
            <TextBlock Text="Ferestrele cu cel mai mare timp total activ." Foreground="#8EA0C3"/>
            <lvc:CartesianChart Series="{Binding WindowUsageSeries}"
                                XAxes="{Binding WindowXAxis}"
                                MinHeight="300"/>
          </StackPanel>
        </Border>

        <Border Grid.Column="1"
                CornerRadius="24"
                Padding="18"
                Background="#10192A"
                BorderBrush="#24324A"
                BorderThickness="1">
          <StackPanel Spacing="12">
            <TextBlock Text="Cronologie sesiuni recente" FontSize="18" FontWeight="Bold" Foreground="#F7FAFF"/>
            <TextBlock Text="Duratele ultimelor sesiuni in minute." Foreground="#8EA0C3"/>
            <lvc:CartesianChart Series="{Binding SessionTimelineSeries}"
                                XAxes="{Binding SessionTimelineXAxis}"
                                MinHeight="300"/>
          </StackPanel>
        </Border>

      </Grid>

      <Grid ColumnDefinitions="*,*" ColumnSpacing="18">

        <Border CornerRadius="24"
                Padding="18"
                Background="#10192A"
                BorderBrush="#24324A"
                BorderThickness="1">
          <StackPanel Spacing="12">
            <TextBlock Text="Defalcare categorii" FontSize="18" FontWeight="Bold" Foreground="#F7FAFF"/>
            <TextBlock Text="Privire rapida asupra categoriilor care conduc volumul de lucru de azi." Foreground="#8EA0C3"/>
            <ItemsControl ItemsSource="{Binding CategoryBreakdown}" ItemTemplate="{StaticResource BreakdownTemplate}"/>
          </StackPanel>
        </Border>

        <Border Grid.Column="1"
                CornerRadius="24"
                Padding="18"
                Background="#10192A"
                BorderBrush="#24324A"
                BorderThickness="1">
          <StackPanel Spacing="12">
            <TextBlock Text="Monitorizare praguri" FontSize="18" FontWeight="Bold" Foreground="#F7FAFF"/>
            <TextBlock Text="Presiunea curenta fata de cele mai relevante limite." Foreground="#8EA0C3"/>
            <ItemsControl ItemsSource="{Binding ThresholdStatuses}" ItemTemplate="{StaticResource ThresholdTemplate}"/>
          </StackPanel>
        </Border>

      </Grid>

      <Grid ColumnDefinitions="*,*" ColumnSpacing="18">

        <Border CornerRadius="24"
                Padding="18"
                Background="#10192A"
                BorderBrush="#24324A"
                BorderThickness="1">
          <StackPanel Spacing="12">
            <TextBlock Text="Domenii browser principale" FontSize="18" FontWeight="Bold" Foreground="#F7FAFF"/>
            <TextBlock Text="Cele mai comune domenii din activitatea browser capturata." Foreground="#8EA0C3"/>
            <ItemsControl ItemsSource="{Binding TopDomains}" ItemTemplate="{StaticResource DomainTemplate}"/>
          </StackPanel>
        </Border>

        <Border Grid.Column="1"
                CornerRadius="24"
                Padding="18"
                Background="#10192A"
                BorderBrush="#24324A"
                BorderThickness="1">
          <StackPanel Spacing="12">
            <TextBlock Text="Interventii recente" FontSize="18" FontWeight="Bold" Foreground="#F7FAFF"/>
            <TextBlock Text="Cele mai noi evenimente de prag si tipul lor configurat de actiune." Foreground="#8EA0C3"/>
            <ItemsControl ItemsSource="{Binding RecentInterventions}" ItemTemplate="{StaticResource InterventionTemplate}"/>
          </StackPanel>
        </Border>

      </Grid>

    </StackPanel>
  </ScrollViewer>
</UserControl>
